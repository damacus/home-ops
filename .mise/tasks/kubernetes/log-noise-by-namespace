#!/usr/bin/env bash
#MISE description="Identify top log-producing namespaces by volume (requires Loki)"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec task "kubernetes:log-noise-by-namespace" -- "$@"
