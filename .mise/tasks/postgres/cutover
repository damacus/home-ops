#!/usr/bin/env bash
#MISE description="Stop blue writes, sync sequences, hand off to operator for the HelmRelease edit"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/pg-bluegreen.sh" cutover "$@"
