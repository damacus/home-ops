#!/usr/bin/env bash
#MISE description="Install workstation dependencies with Brew"
#MISE quiet=true
#MISE raw=true
set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." >/dev/null 2>&1 && pwd)"
exec brew bundle --file "${repo_root}/Brewfile"
