#!/usr/bin/env bash
#MISE description="Clean files and directories no longer needed after cluster bootstrap"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "repository:clean" "$@"
