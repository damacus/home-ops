#!/usr/bin/env bash
#MISE description="Validate prerequisites before standing up green"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-postgres-task.sh" preflight "$@"
