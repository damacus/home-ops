---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmchart_v1.json
apiVersion: helm.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: reflector
  namespace: reflector
spec:
  chart: reflector
  version: 7.1.306
  sourceRef:
    kind: OCIRepository
    name: emberstack
    namespace: flux-system
  interval: 1h
