#!/usr/bin/env bash
#MISE description="Reset repo back to HEAD"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "repository:force-reset" "$@"
