#!/usr/bin/env bash
#MISE description="Revert to blue (git revert + flux reconcile)"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/pg-bluegreen.sh" rollback "$@"
