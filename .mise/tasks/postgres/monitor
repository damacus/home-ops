#!/usr/bin/env bash
#MISE description="Print pub/sub status + replication lag (one-shot)"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/pg-bluegreen.sh" monitor "$@"
