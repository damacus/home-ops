#!/usr/bin/env bash
#MISE description="Check unsuspended Flux sources and releases are Ready"
#MISE quiet=true
#MISE raw=true
#MISE depends=["kubernetes:cluster-health-build"]
set -euo pipefail
exec .cache/bin/cluster-health gitops-health "$@"
