#!/usr/bin/env bash
#MISE description="Check active pods do not reference deleted ServiceAccounts"
#MISE quiet=true
#MISE raw=true
#MISE depends=["kubernetes:cluster-health-build"]
set -euo pipefail
exec .cache/bin/cluster-health service-account-health "$@"
