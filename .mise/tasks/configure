#!/usr/bin/env bash
#MISE description="Configure repository from bootstrap vars"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../scripts/mise-task-facade.sh" "configure" "$@"
