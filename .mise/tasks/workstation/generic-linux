#!/usr/bin/env bash
#MISE description="Install CLI tools into the projects .bin directory using curl"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "workstation:generic-linux" "$@"
