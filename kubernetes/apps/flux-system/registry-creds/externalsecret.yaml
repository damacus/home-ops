---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dockerhub-creds
  namespace: flux-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: dockerhub-creds
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "docker.io": {
                "username": "{{ .DOCKERHUB_USERNAME }}",
                "password": "{{ .DOCKERHUB_PASSWORD }}",
                "auth": "{{ list .DOCKERHUB_USERNAME .DOCKERHUB_PASSWORD | join \":\" | b64enc }}"
              }
            }
          }
      type: kubernetes.io/dockerconfigjson
  dataFrom:
    - extract:
        key: dockerhub
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ghcr-creds
  namespace: flux-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: ghcr-creds
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "ghcr.io": {
                "username": "{{ .GHCR_USERNAME }}",
                "password": "{{ .GHCR_TOKEN }}",
                "auth": "{{ list .GHCR_USERNAME .GHCR_TOKEN | join \":\" | b64enc }}"
              }
            }
          }
      type: kubernetes.io/dockerconfigjson
  dataFrom:
    - extract:
        key: github-container-registry
