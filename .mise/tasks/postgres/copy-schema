#!/usr/bin/env bash
#MISE description="Schema-only pg_dump from blue piped into psql on green"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-postgres-task.sh" copy-schema "$@"
