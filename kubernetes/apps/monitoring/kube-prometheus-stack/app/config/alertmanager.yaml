---
alertmanager:
  ingress:
    enabled: true
    spec:
      ingressClassName: internal
    annotations:
      reloader.stakater.com/auto: "true"
      external-dns.alpha.kubernetes.io/target: ${NGINX_INTERNAL_ADDR}
    hosts: ["alertmanager.home.arpa"]
    paths: ["/"]
    tls:
      - hosts: ["alertmanager.home.arpa"]
        secretName: alert-manager-${SECRET_DOMAIN}-cert
