#!/usr/bin/env bash
#MISE description="Report deployment availability"
#MISE quiet=true
#MISE raw=true
#MISE depends=["kubernetes:cluster-health-build"]
set -euo pipefail
exec .cache/bin/cluster-health deployments "$@"
