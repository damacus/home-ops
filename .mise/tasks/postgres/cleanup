#!/usr/bin/env bash
#MISE description="After soak: drop pub/sub, delete blue cluster, clear runtime state"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/pg-bluegreen.sh" cleanup "$@"
