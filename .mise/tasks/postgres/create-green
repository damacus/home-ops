#!/usr/bin/env bash
#MISE description="Apply the green CNPG cluster manifest and wait for healthy"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-postgres-task.sh" create-green "$@"
