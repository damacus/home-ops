#!/usr/bin/env bash
#MISE description="Copy or sync objects between MinIO and RustFS with local rclone"
#MISE quiet=true
#MISE raw=true
set -euo pipefail

exec "$(dirname "$0")/../../../scripts/object_storage_migrate.sh" "$@"
