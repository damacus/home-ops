---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: dex
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: dex-secret
  dataFrom:
    - extract:
        key: dex-google
      rewrite:
        - regexp:
            source: "(.*)" # regex to match the entire string
            target: "DEX_GOOGLE-$1" # prepend "GITHUB-" to the matched string
    - extract:
        key: dex-github
      rewrite:
        - regexp:
            source: "(.*)" # regex to match the entire string
            target: "DEX_GITHUB-$1" # prepend "GITHUB-" to the matched string
    - extract:
        key: dex-oauth
      rewrite:
        - regexp:
            source: "(.*)" # regex to match the entire string
            target: "DEX_OAUTH2-$1" # prepend "oauth2-" to the matched string
