#!/usr/bin/env bash
#MISE description="Generate the Unifi keystore"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "certificates:generate-unifi-keystore" "$@"
