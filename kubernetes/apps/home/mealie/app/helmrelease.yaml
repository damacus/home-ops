---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mealie
  namespace: home
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 5.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      mealie:
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/mealie-recipes/mealie
              tag: v3.17.0@sha256:d570139f1891346131d02f8e4d1853100ae4846474958ca6c68cfe1648058467
            env:
              ALLOW_SIGNUP: "false"
              BASE_URL: "https://{{ .Release.Name }}.damacus.io"
              DB_ENGINE: postgres
              DEFAULT_GROUP: "Home"
              DEFAULT_HOUSEHOLD: "Ironstone"
              MAX_WORKERS: 1
              OIDC_AUTH_ENABLED: "true"
              OIDC_AUTO_REDIRECT: "false"
              OIDC_CLIENT_ID:
                valueFrom:
                  secretKeyRef:
                    name: mealie-zitadel-oidc
                    key: client-id
              OIDC_CLIENT_SECRET:
                valueFrom:
                  secretKeyRef:
                    name: mealie-zitadel-oidc
                    key: client-secret
              OIDC_CONFIGURATION_URL: "https://zitadel.damacus.io/.well-known/openid-configuration"
              OIDC_GROUPS_CLAIM: "groups"
              OIDC_NAME_CLAIM: "name"
              OIDC_PROVIDER_NAME: "Zitadel"
              OIDC_REMEMBER_ME: "false"
              OIDC_SCOPES: "openid profile email groups"
              OIDC_SIGNUP_ENABLED: "false"
              OIDC_USER_CLAIM: "email"
              OPENAI_API_KEY:
                valueFrom:
                  secretKeyRef:
                    name: mealie-secret
                    key: OPENAI_API_KEY
              PGID: 1000
              POSTGRES_DB: "{{ .Release.Name }}"
              POSTGRES_PORT: "5432"
              POSTGRES_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: mealie-secret
                    key: POSTGRES_PASSWORD
              POSTGRES_SERVER: "mealie-green-rw"
              POSTGRES_USER: "{{ .Release.Name }}"
              PUID: 1000
              TZ: "Europe/London"
              WEB_CONCURRENCY: 1
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
            resources:
              requests:
                cpu: 10m
                memory: 250Mi
              limits:
                memory: 1Gi
        pod:
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile: {type: RuntimeDefault}
    route:
      app:
        annotations:
          external-dns.alpha.kubernetes.io/target: "traefik-ext.damacus.io"
        parentRefs:
          - name: traefik-internal
            namespace: network
            sectionName: websecure
        hostnames:
          - "{{ .Release.Name }}.damacus.io"
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - kind: Service
                name: mealie
                namespace: home
                port: 9000
                weight: 1
    service:
      app:
        controller: mealie
        ports:
          http:
            port: 9000
    persistence:
      data:
        existingClaim: mealie-data
        globalMounts:
          - path: /app/data
