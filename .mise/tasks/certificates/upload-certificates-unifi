#!/usr/bin/env bash
#MISE description="Upload the certificates to Unifi"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "certificates:upload-certificates-unifi" "$@"
