#!/usr/bin/env bash
#MISE description="Validate Kubernetes manifests with yayamlls and rendered Flux output"
#MISE quiet=true
#MISE raw=true
set -euo pipefail

command -v yayamlls >/dev/null || { printf 'Missing yayamlls\n' >&2; exit 1; }
exec yayamlls validate --render "$PWD/kubernetes"
