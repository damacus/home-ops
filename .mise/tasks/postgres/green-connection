#!/usr/bin/env bash
#MISE description="Print the green cluster connection URI (reads n8n-green-app)"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-postgres-task.sh" green-connection "$@"
