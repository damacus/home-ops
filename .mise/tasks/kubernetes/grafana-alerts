#!/usr/bin/env bash
#MISE description="Check firing Grafana/Prometheus and Alertmanager alerts"
#MISE quiet=true
#MISE raw=true
#MISE depends=["kubernetes:cluster-health-build"]
set -euo pipefail
exec .cache/bin/cluster-health grafana-alerts "$@"
