---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minio
spec:
  interval: 5m
  chart:
    spec:
      chart: operator
      version: 7.0.1
      sourceRef:
        kind: HelmRepository
        name: minio-operator
        namespace: flux-system
