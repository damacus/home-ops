#!/usr/bin/env bash
#MISE description="Download the certificates from the cluster"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "certificates:download-certificates" "$@"
