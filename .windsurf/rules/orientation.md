---
trigger: model_decision
---

# Orientation

When you need to orient yourself with the current state of the cluster, run `mise run kubernetes:resources` to get a list of all running pods and their status.
