#!/usr/bin/env bash
#MISE description="Check active pods do not reference deleted ServiceAccounts"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec task "kubernetes:service-account-health" -- "$@"
