#!/usr/bin/env bash
#MISE description="Check all CNPG clusters for healthy ready state"
#MISE quiet=true
#MISE raw=true
#MISE depends=["kubernetes:cluster-health-build"]
set -euo pipefail
exec .cache/bin/cluster-health cnpg-health "$@"
