---
grafana:
  ingress:
    enabled: true
    spec:
      ingressClassName: traefik-external
    annotations:
      reloader.stakater.com/auto: "true"
      external-dns.alpha.kubernetes.io/target: ${TRAEFIK_EXTERNAL_ADDR}
    hosts: ["grafana.${SECRET_DOMAIN}"]
    paths: ["/"]
    tls:
      - hosts: ["grafana.${SECRET_DOMAIN}"]
        secretName: grafana-${SECRET_DOMAIN}-cert
