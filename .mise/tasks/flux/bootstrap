#!/usr/bin/env bash
#MISE description="Bootstrap Flux into a Kubernetes cluster"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "flux:bootstrap" "$@"
