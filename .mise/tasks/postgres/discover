#!/usr/bin/env bash
#MISE description="Snapshot current CNPG/app/db state"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-postgres-task.sh" discover "$@"
