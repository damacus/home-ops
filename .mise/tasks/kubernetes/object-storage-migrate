#!/usr/bin/env bash
#MISE description="Copy or sync objects between MinIO and RustFS with local rclone"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "kubernetes:object-storage-migrate" "$@"
