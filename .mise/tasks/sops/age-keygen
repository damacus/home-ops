#!/usr/bin/env bash
#MISE description="Initialize Age Key for Sops"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "sops:age-keygen" "$@"
