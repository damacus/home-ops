#!/usr/bin/env bash
#MISE description="Apply a Flux Kustomization resource for a cluster"
#MISE quiet=true
#MISE raw=true
set -euo pipefail
exec "$(dirname "$0")/../../../scripts/mise-task-facade.sh" "flux:apply" "$@"
